<h1>{{title}}</h1>

<div class="container">
  <div class="row g-2">
    {{#each groups}}
      <div class="col-12 col-sm-6 col-md-4 col-lg-3">
        <a href="/kafs/{{id}}" class="btn btn-outline-primary w-100">{{name}}</a>
      </div>
    {{/each}}
  </div>
</div>

